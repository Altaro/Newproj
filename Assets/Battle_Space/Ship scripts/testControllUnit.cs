using UnityEngine;
using System.Collections;

public class testControllUnit : MonoBehaviour {

	
	// Use this for initialization
	void Start () {
	GetComponent<enginesControl>().moveFront(50);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
